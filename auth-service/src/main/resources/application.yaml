spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:oracle:thin:@oracle.fiap.com.br:1521/orcl
    username: rm560077
    password: 300903
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.OracleDialect

server:
  port: 8081

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: orbitbook-secret-key-orbitbook-secret-key-2026
  expiration: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info